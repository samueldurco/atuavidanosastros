# Private PDF export — WU-038

RUN_ID: `ATV-20260902-170644Z-01A0630F`. Local implementation and synthetic QA, not a production product release or model homologation.

## Delivered boundary

- Real PDF 1.7 bytes from the currently authorized released projection, through the existing same-origin owner download route. Catalog eligibility, fresh access checks, attachment disposition, private/no-store, sandbox CSP, export version and SHA-256 are enforced. No raw input, service-role read, external fetch or executable link/markup enters the document.
- Full reading and evidence, calculated/reported facts, sources, version labels, method limitations and revision history. A4 pagination, measured wrapping, long-word handling and page counters use embedded brand fonts. Source text is not editorially rewritten.
- Eligible reader action with busy/error recovery and MIME verification. Synthetic local fixtures disable downloads; unavailable/revoked runs do not expose the action. Existing HTML downloads remain covered.
- Fail-closed bounds: 120,000 projected JSON characters, 40 pages, 8 MB, cooperative five-second elapsed check. Missing font glyphs (including unsupported emoji/CJK), invalid controls and exceeded limits do not produce a partial report.

## Verification

- `pnpm test:unit`: **132 PASS** (11 domain, 20 AI, 23 astrology, 3 integrations, 36 worker, 39 web). The web suite includes five PDF renderer tests and nine shared download tests. Real bytes, deterministic metadata, hash/headers, catalog refusal, revocation/deletion/session expiry, long text, unsupported glyphs and budgets are exercised. Logs: `test-results/wu038-unit.log`, `wu038-pdf-unit.log`.
- `pnpm check`: **PASS**, zero Svelte errors/warnings; `pnpm lint`: **PASS**. Logs: `test-results/wu038-check.log`, `wu038-lint.log`.
- Local Playwright build/preview and focal reader/export suite: **14 PASS**. Reader at 1440/820/390/320 px; PDF action, no horizontal overflow, synthetic restrictions and revoked states; existing offline HTML suite. Log: `test-results/wu038-e2e.log`. All four reader widths visually inspected; ReadingShell and MEM-03 reference `58afdd10c4b7484d8167ea5c62b29ee3` retained.
- Four-page synthetic PDF: **19,873 bytes**, SHA-256 `dbe06383d9b2847875cc91ae84f89fbaac8f7bcfd254ff8b7b578aa1641ca86a`. Generated by `ATV_PDF_QA=1` renderer test into `test-results/pdf/`; all four Poppler-rendered pages visually inspected. No clipping/overlap; titles, body, fact sources and revision appendix remain legible. Final code-only control validation fix preserved identical PDF bytes.
- `scripts/verify-product-pdf.py`: **PASS** via pypdf/pdfplumber. Checks full normalized section/fact/source text, page furniture, character bounds, metadata and absence of active annotations/actions/raw-input canary. Intermediate PDF, JSON and PNGs are ignored synthetic QA files, not real customer results.
- No SQL changed: WU-037's 22 local PostgreSQL tests are prior evidence, not rerun or claimed as hosted integration here. Prior commit `54a4856` CI confirmed: quality `104161805425`, secrets `104161805487`, Pages `104162196552`, all completed/success.

## Corrections and reproducibility

Variable WOFF2 subsetting failed on serialization in pinned `@pdf-lib/fontkit` 1.1.1. PDF-only static TrueType instances are now reproducibly derived from the existing brand fonts using `scripts/build-pdf-fonts.py` (fonttools 4.65.0/brotli 1.2.0); website originals remain unchanged. Adjacent OFL 1.1 notices and provenance are versioned. Renderer uses pinned pdf-lib 1.17.1 and fontkit 1.1.1, with inline server font data and no runtime font service. Metadata checks load PDFs with `updateMetadata: false`, preventing the test reader from replacing the date being asserted.

## Remaining gates

Generated-on-demand PDF is not a persisted artifact manifest, signed download URL, e-mail delivery or reprocessing scheduler. Copies already downloaded cannot be revoked; future requests recheck access. No full chart, SVG, audio, PDF/A, PDF/UA, signature or encryption claim. A cooperative deadline cannot interrupt synchronous font serialization; hosted CPU/memory/load-budget verification and JWT/PostgREST integration remain mandatory before rollout. Unsupported-script typography requires separately licensed/tested font coverage, not silent substitution.

All 25 workflow releases remain disabled in checked-in migrations. The model promotion registry stays empty. No paid call, hosted migration, editorial publication or new spend was authorized by this WU. Ten partial calculation bases are still not six completed vertical product families.
