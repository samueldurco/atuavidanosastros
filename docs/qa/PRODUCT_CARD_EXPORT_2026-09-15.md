# Private section cards — WU-040

RUN_ID: `ATV-20260902-170644Z-01A0630F`. Local implementation and synthetic QA only. No model or product homologation.

## Delivered boundary

- Complete selected editorial section, every cited fact and every calculation/editorial limit. No new AI call, summarization, unrelated-section facts or raw input. Minimal run/revision/section/review provenance. Web-eligible released catalog products only; no new entitlement.
- Strict single format and canonical section parameter, fresh authenticated owner read, current publication/revocation gates, attachment, private/no-store, CSP, nosniff and SHA-256. Inert escaped SVG with three embedded existing brand fonts and no external requests or active elements.
- Full text wraps using font metrics on a 1080-pixel-wide canvas. 24,000 projected JSON characters, 8192-pixel height, 2 MB and cooperative five-second budget. Unsupported glyphs and oversized content fail closed without truncation. Hosted resource limits remain unverified.
- Reader section selector and recoverable private download action. Controls are hidden for revoked/unreleased results and downloads remain disabled on local synthetic fixtures. Explicit whole-reading and offline-copy limitations; no automatic public sharing or persisted artifact manifest.

## Verification

- `pnpm test`: **151 PASS** (11 domain, 20 AI, 23 astrology, 3 integrations, 36 worker, 58 web). Eight card renderer tests; shared download suite now twelve tests. Deterministic complete content, selected evidence, inert hostile input, malformed/revoked/unsupported cases, strict query parsing, fresh authorization, deadlines and byte/hash headers covered. Log: `test-results/wu040-final-unit.log`.
- `pnpm check`: **PASS**, zero Svelte errors/warnings. `pnpm lint`: **PASS** after adding the required keyed section options. Logs: `test-results/wu040-final-check.log`, `wu040-final-lint.log`.
- Local build/preview and focal Playwright: **28 PASS**, including existing HTML/PDF/SVG regressions. Two standalone cards verify loaded fonts, complete content, no external requests, text bounds and non-overlap; four reader widths 1440/820/390/320 verify selection, revoked/synthetic states and no overflow. Log: `test-results/wu040-e2e-final.log`.
- All six new captures visually inspected: standard card 1080×1505, long card 1080×2490 and four reader widths. Initial geometry test detected title overlap; renderer line spacing was corrected using font metrics and top-aligned text. Assertions were retained and the full focal suite passed. Screenshots are ignored under `apps/web/test-results/`. MEM-03 ReadingShell reference `58afdd10c4b7484d8167ea5c62b29ee3` retained; visual approval is scoped, not a full six-universe Gate B.
- No SQL change in this WU; the previous 28 local database tests are not represented as a new run. Previous SHA `f87df0c`: quality `104532762805`, secrets `104532762577` and Pages `104533197361` completed/success via dedicated GitHub API.

## Remaining gates

No hosted migration, paid call, release activation or model promotion. All 25 workflow releases remain disabled. Cards, HTML, PDF and cartography are generated on demand; persistent artifact manifests, eligible audio, email delivery, complete premium cartography and six completed verticals remain separate work. Hosted JWT/resource-budget verification and integral engine/editorial approvals remain mandatory.
