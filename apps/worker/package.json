{
  "name": "@atv/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "node --test",
    "test:unit": "node --test"
  },
  "dependencies": {
    "@atv/domain": "workspace:*",
    "@atv/integrations": "workspace:*",
    "@atv/observability": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
